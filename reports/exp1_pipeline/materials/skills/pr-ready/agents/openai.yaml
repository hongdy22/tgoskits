interface:
  display_name: "PR Ready"
  short_description: "Prepare a StarryOS fix for upstream PR review"
  default_prompt: "Check whether this StarryOS fix is ready for an upstream PR and draft the PR body."
